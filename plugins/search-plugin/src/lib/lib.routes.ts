import { Route } from '@angular/router';
import { SearchPluginComponent } from './search-plugin/search-plugin.component';

export const searchPluginRoutes: Route[] = [{ path: '', component: SearchPluginComponent }];
